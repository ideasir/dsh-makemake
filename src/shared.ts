/** Values shared by the Host and browser Bundle faces. */

/** Browser route used by the generated-image card. */
export const IMAGE_ROUTE = '/plugins/dsh-makemake/image'

/** Browser route used by the generated-video card (same-origin proxy for video bytes). */
export const VIDEO_ROUTE = '/plugins/dsh-makemake/video'

/** Browser route that persists a generated video to .uploads/ and returns its `[f:file]` tag. */
export const VIDEO_REF_ROUTE = '/plugins/dsh-makemake/video-ref'

/** Namespace persisted through DSH Settings. */
export const CREATION_NAMESPACE = 'creation'